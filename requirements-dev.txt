# 开发/测试依赖（**不要**装进生产镜像）
#
# 生产镜像只装 requirements.txt，且发布流程是 docker cp 覆盖代码、不重建镜像，
# 因此这里的东西一旦混进 requirements.txt，会变成"新增依赖永远进不了容器"的坑。
#
# 安装：
#   pip install -r requirements-dev.txt
# 用法：
#   python scripts/quality_gate.py          # 结构检查 + 自测脚本 + pytest
#   python -m pytest -q                     # 只跑 pytest 用例
pytest>=8.0
pytest-timeout>=2.3
fakeredis>=2.20
