# 销售系统环境体检报告

> 检查日期：2026-09-15  
> 检查范围：本机运行时、项目代码、SQLite、微信桥接、Electron 客户端、云端服务

## 一、总体结论

本机开发环境和代码质量正常，但当前环境处于版本错层状态：

| 层级 | 当前状态 |
|---|---|
| 本机源码 | 客户端 `1.0.11`，桥接 `2026.09.15.1` |
| 已安装客户端 | `1.0.10` |
| 已安装桥接 | `2026.09.14.10` |
| 云端迁移版本 | `0009_reply_reliability` |
| 当前代码迁移版本 | `0010_commercial_hardening` |

结论：必须先升级云端到 `0010`，再更新本机客户端和桥接。现在直接使用 `1.0.11` 会遇到新接口不存在或权限行为不一致。

## 二、本机运行环境

| 项目 | 检查结果 |
|---|---|
| Windows | 正常 |
| Python 3.11 | `3.11.9`，依赖完整，推荐使用 |
| Python 3.12 | 已安装，但没有项目依赖，不应使用 |
| `langchain_classic` | 已安装，版本 `1.0.8` |
| FastAPI | `0.139.2` |
| SQLAlchemy | `2.0.51` |
| Pydantic | `2.13.4` |
| Node.js | `24.18.0` |
| npm | `11.16.0` |
| Electron | `33.4.11` |
| VS Code | `1.137.0` |
| Docker Desktop | 本机未安装，不影响云端运行，但无法在本机复现容器环境 |

VS Code 已通过项目级配置固定到：

```text
C:\Program Files\Python311\python.exe
```

## 三、本机配置

| 配置 | 状态 |
|---|---|
| 本地数据库 | SQLite |
| `AUTO_CREATE_TABLES` | `true` |
| Redis | 未配置，使用内存模式 |
| 缓存 Redis | 未配置 |
| 普通 API Key | 已配置 |
| 管理员 API Key | 已配置 |
| API Key 与租户映射 | 未配置，默认租户 0 |
| 支付回调密钥 | 未配置，支付回调会被拒绝 |
| RAG 后端 | `auto`，本机无 PostgreSQL 时回退 JSON |

## 四、本地 SQLite

数据库完整性正常：

```text
integrity_check = ok
foreign_key_issues = 0
```

当前数据量：

| 表 | 数量 |
|---|---:|
| 客户 | 8 |
| 线索 | 13 |
| 订单 | 3 |
| 渠道会话 | 8 |
| 渠道消息 | 18 |
| 微信回复任务 | 9 |
| 知识文档 | 0 |
| 知识切片 | 0 |
| 服务账号 Key | 0 |

存在的问题：

1. 本地库没有 `alembic_version`，未纳入迁移管理。
2. `customers` 已有 `tenant_id`，但部分历史表仍缺少该列。
3. `AUTO_CREATE_TABLES=true` 只能新建表，不能给已有表补列。
4. 当前本地库不适合直接承接新的多租户代码。

需要先备份，再执行：

```powershell
Copy-Item data\customers.db data\customers.db.bak-环境体检-20260915
$env:DATABASE_URL="sqlite:///C:/Users/1/Documents/销售客服智能体/data/customers.db"
$env:AUTO_CREATE_TABLES="false"
python -m alembic stamp 0006_add_display_id
python -m alembic upgrade head
```

## 五、微信桥接

当前状态：

| 项目 | 状态 |
|---|---|
| 微信进程 | 运行中 |
| 微信窗口 | 已检测到 |
| 桥接守护进程 | 运行中 |
| 桥接子进程 | 运行中 |
| 云中心地址 | `https://your-domain.example` |
| 实例号 | `desktop-75a80bd4d9` |
| 云端在线状态 | 在线 |
| 平台账号识别 | 正常 |
| 读取器 | 已就绪 |
| 客户接待开关 | 关闭 |
| 微信登录检测开关 | 关闭 |

当前云端任务状态：

| 状态 | 数量 |
|---|---:|
| 已发送 | 2 |
| 待人工处理 | 2 |
| 失败 | 2 |

待关注任务原因：

- 搜索结果中没有精确匹配的目标。
- 打开会话后的标题与目标不一致。
- 发送后没有回读到微信发送结果。

这说明桥接在线，但当前不会自动识别和回复，因为客户接待开关关闭。旧版桥接和旧版云端还造成了昵称字段不完整。

## 六、本地服务

以下端口当前均未监听：

```text
5000
5010
39123
8080
6379
5432
3900
```

本机没有运行本地 API 或本地 PostgreSQL/Redis。当前桥接实际使用云端 `https://your-domain.example`，所以本地 API 未启动本身不是故障。

## 七、云端服务

| 检查项 | 结果 |
|---|---|
| 域名 | `your-domain.example` |
| IP | `<your-server-ip>` |
| `/health` | HTTP 200，返回 `{"status":"ok"}` |
| `/ready` | HTTP 200 |
| PostgreSQL | 正常 |
| Redis | 正常 |
| 数据库迁移 | 正常 |
| 云端迁移版本 | `0009_reply_reliability` |

云端当前是旧接口：

```json
{"ok":true,"note":"API Key 模式"}
```

这说明 `/api/v1/accounts/me` 仍是旧版本，不包含角色、租户和权限列表。

## 八、需要立即处理

### P0

1. 先部署 `cloud-commercial-hardening-20260915-v2.zip`。
2. 确认 `/ready` 的迁移版本变为 `0010_commercial_hardening`。
3. 配置 `PAYMENT_WEBHOOK_SECRET`。
4. 配置 `API_KEY_TENANTS`，不要继续默认落租户 0。
5. 升级本机客户端到 `1.0.11`。
6. 重启微信桥接，使其使用 `2026.09.15.1`。
7. 打开客户接待和微信登录检测开关。

### P1

1. 处理两条待人工复核任务和两条失败任务。
2. 检查微信搜索结果中出现多个同名或相似联系人时的选择策略。
3. 统一云端时间显示。当前云端 `last_seen` 表现为 UTC，本地显示为北京时间，存在 8 小时差异。
4. 本机 SQLite 在测试新多租户代码前执行迁移。
5. 配置 Windows 代码签名证书后再对外分发客户端。

## 九、建议执行顺序

```text
备份云端和本地数据库
→ 配置服务器 .env
→ 部署云端加固包
→ 验证 /ready = 0010
→ 升级本机客户端到 1.0.11
→ 重启桥接并确认版本 2026.09.15.1
→ 打开接待和登录检测
→ 发送一条真实测试消息
→ 检查云端任务、前端状态和微信回执
```
