# 视觉执行器 · 企业商用操作手册

## 1. 这套东西是什么

视觉执行器是仿识流 AI 的受管桌面 Agent：它运行在已经登录企业微信/个人微信 PC 的 Windows 电脑上，通过截图 + 视觉模型识别会话界面，像真人一样点击、输入、发送。服务端中台负责统一调度、去重、风控、审计和人工接管。

默认关闭，管理员显式授权后才开始执行；命中风控关键词、发送超限、熔断时自动暂停并留审计。

边界说明：这套功能与“企业微信微信客服”完全独立，不使用微信客服 Secret、不使用官方回调，也不依赖网页客服。它只运行在普通企业微信个人账号或普通个人微信号的 PC 客户端上，通过界面自动化操作，属于全新的“自动化通道”模块。

## 2. 覆盖能力

- 自动回复：识别未读会话并回复客户消息
- 主动触达：中台下发任务，桌面端打开联系人并发送
- 定时触达：任务可指定执行时间
- 消息去重：同一消息只回复一次，服务端 + 本地双重指纹去重
- 上下文一致：每个联系人使用独立会话 ID，走同一套销售 Agent 记忆
- 转人工接管：客户要求人工时自动标记并停止自动回复，管理员可恢复
- 群聊规则：默认只回复命中触发关键词的群消息
- 多开：每台电脑一个实例 ID，可独立暂停/恢复
- 审计：每次截图、识别、发送、暂停、任务流转全部留痕

## 3. 服务端部署

1. 把包含以下文件的压缩包上传到服务器根目录 `/enterprise-vision-20260828.zip`：
   - `core/vision.py`
   - `api/vision.py`
   - `config/settings.py`
   - `api_server.py`
   - `static/console.html`
   - `static/vision_console.html`
   - `deploy/apply_vision_round.sh`
   - `deploy/verify_vision_round.sh`
2. 执行：
   ```bash
   bash /opt/sales-agent/deploy/apply_vision_round.sh
   bash /opt/sales-agent/deploy/verify_vision_round.sh
   ```
3. 中台打开“渠道接入 -> 视觉执行器”，填 API Key 后看到在线状态即部署成功。

## 4. 桌面端安装

在要运行的 Windows 电脑上：

```bash
cd C:\Users\1\Documents\销售客服智能体
pip install -r vision_agent/requirements.txt
```

编辑 `start_vision_agent.bat`，填写：

| 变量 | 说明 |
|---|---|
| `VISION_AGENT_API_BASE` | 中台地址，如 `https://your-domain.example` |
| `API_KEYS` | 中台 API Key |
| `DASHSCOPE_API_KEY` | 百炼视觉模型 Key（qwen-vl-max） |
| `VISION_AGENT_MODE` | `wecom` 企业微信 / `wechat` 个人微信 |
| `VISION_AGENT_INSTANCE_ID` | 每台电脑唯一，如 `desktop-1` |
| `VISION_AGENT_MONITOR` | 客户端窗口标题，如“企业微信” |
| `VISION_DEBUG_NO_SEND` | `1` 为演练模式，只识别不发送；正式跑改成 `0` |

先用演练模式跑通识别，再切正式发送。

## 5. 日常操作

1. 打开中台 -> 视觉执行器工作台，确认“在线实例”>= 1。
2. 状态总览看今日发送、每日上限、待执行任务。
3. “主动触达”创建任务：选企业微信/个人微信，填联系人显示名和内容，可定时。
4. 任务状态：待执行 -> 执行中 -> 已发送/失败/跳过。
5. 人工接管：客户要求转人工后，该联系人自动停止自动回复；人工处理完点“恢复自动回复”。
6. 审计日志可查所有动作；发现异常先在实例列表暂停。

## 6. 风控红线

- 视觉自动化属于非官方通道，存在风控/封号可能，必须业务书面授权后使用。
- 默认每天 300 条、每小时 60 条、每分钟 5 条、连续 12 条；可调小不可盲目调大。
- 检测到“操作频繁/封号/安全风险”等提示会自动熔断 30 分钟并通知管理员。
- 官方合规通道（企业微信自建应用、微信客服、公众号）不受此风控影响，建议双通道并用。
