# 销售客服智能体 · 全量部署说明

## 一、这个包是什么

这是把整个销售客服智能体（企业级、数据智能、销售流程、自动跟进、企业微信/微信客服、视觉执行器）整合成的一份全量部署包。

包内已经包含：

- 后端代码：`api/`、`core/`、`connectors/`、`config/`、`knowledge/`、`migrations/`、`utils/`
- 前端中台：`static/`（含视觉执行器工作台）
- 服务入口：`api_server.py`、`main.py`
- 容器与部署：`Dockerfile`、`docker-compose.yml`、`deploy/`
- 配置示例：`.env.example`（不含真实密钥）
- 桌面视觉执行器：`vision_agent/` + 操作手册

包内不会包含：`.env`、数据库文件、日志、向量库、个人微信 node_modules 等环境数据。

## 二、服务器首次部署

1. 把 `sales-agent-full-deploy-20260828.zip` 上传到服务器根目录：

```bash
ls -l /sales-agent-full-deploy-20260828.zip
```

2. 解压到项目目录：

```bash
unzip -o /sales-agent-full-deploy-20260828.zip -d /opt/sales-agent
cd /opt/sales-agent
```

3. 初始化环境变量：

包内已附 `server.env`，包含你之前提供的中台 API Key、DeepSeek/百炼 Key、企业微信和微信客服全套密钥。服务器部署时：若之前没有 `.env`，直接使用；若已有 `.env`，不要覆盖，保留原文件即可。

```bash
cp server.env .env   # 仅新服务器首次部署时使用；已有 .env 则跳过
```

4. 一键部署：

```bash
bash /opt/sales-agent/deploy/apply_all_rounds.sh
```

5. 一键验证：

```bash
bash /opt/sales-agent/deploy/verify_all_rounds.sh
```

看到健康检查 `HTTP:200`，并且各轮次验证 `OK`，即部署完成。

## 三、桌面视觉执行器

视觉执行器不是跑在服务器上的，而是跑在“企业微信/个人微信 PC 客户端已登录”的 Windows 电脑上：

```bash
cd 本机项目目录
pip install -r vision_agent/requirements.txt
```

编辑 `start_vision_agent.bat`，填服务器地址、API Key、百炼 Key，先演练再正式发送。详细步骤见《视觉执行器-企业商用操作手册.md》。

## 四、边界说明

- 企业微信自建应用、微信客服：官方 API 通道，合规兜底。
- 视觉执行器：普通企业微信个人号/个人微信号 PC 客户端的自动化通道，与微信客服完全独立。
- 视觉执行器默认关闭，需管理员授权；有风控、审计、熔断和人工接管。
