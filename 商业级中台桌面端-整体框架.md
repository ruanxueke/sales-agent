# 商业级中台桌面端 · 整体框架

> 定位：基于现有“销售客服智能体”，升级为可对外分发、多租户隔离、登录后仅见自己数据的企业级 SaaS 桌面应用。
> 视觉执行器不再是独立黑框程序，而是桌面客户端内的一个“可开关能力”。

---

## 1. 产品形态

- 官网提供 Windows `.exe` 下载，可免费试用/付费激活。
- 打开应用进入登录页，支持账号密码 / 企业邀请码 / 微信扫码（后续）。
- 登录后进入该企业自己的中台，所有客户、线索、订单、BI、工作流、知识库均为本租户数据。
- 中台内提供“视觉执行器”开关：开启后自动回复企业微信/个人微信 PC 消息，关闭立即停止。
- 无黑框、无命令行、无本地数据库直连；桌面端只是“操作客户端 + 视觉执行器”。

---

## 2. 总体架构

```text
官网/下载站
  ↓ 下载 .exe
桌面客户端（Windows）
  ├─ 登录/激活
  ├─ 多租户中台界面（仅本租户数据）
  └─ 视觉执行器（可开关、可审计、可熔断）
  ↓ HTTPS + Token
API 网关（鉴权 / 限流 / 审计 / IP 白名单）
  ↓
SaaS 服务端
  ├─ 租户/账号/RBAC
  ├─ 业务引擎（销售 Agent、CRM、知识库、BI、流程、企微/微信客服）
  ├─ 视觉执行器服务端控制（状态/任务/去重/风控/审计）
  ├─ 计费/用量/许可证
  └─ 运维（监控/告警/备份/升级）
  ↓
PostgreSQL（多租户） + Redis（会话/队列） + 对象存储（文件/知识库）
```

---

## 3. 各层设计

### 3.1 官网与分发

- 品牌官网：产品介绍、功能页、价格页、下载页、帮助中心。
- 下载服务：直链 + CDN，提供版本号、哈希校验。
- 自动更新：客户端启动时检查更新，静默下载、重启安装、失败回滚。
- 激活流程：下载 → 安装 → 登录 → 绑定租户 → 授权生效。

### 3.2 账号与租户

- 账号表：用户、密码哈希、手机/邮箱、状态。
- 租户表：企业名、套餐、状态、过期时间、IP 白名单。
- 成员表：用户 ↔ 租户 ↔ 角色（管理员/经理/客服/只读）。
- 登录：返回 `access_token`（短时）+ `refresh_token`（长时），服务端可吊销。
- 首次登录：注册企业 → 创建管理员 → 邀请成员 → 进入工作台。

### 3.3 桌面客户端

- 技术路线：
  - MVP：Python + pywebview + PyInstaller，快速验证。
  - 商用正式版：Electron / Tauri + React，界面和更新体验更接近商业产品。
- 本地能力：
  - 安全存储 API Key / Token（Windows DPAPI 加密）。
  - 视觉执行器：截屏识别、模拟操作、本地去重、频控、熔断。
  - 日志：本地滚动日志，可一键上传给管理员排查。
  - 多开/多账号：每台电脑一个实例 ID，可绑定不同租户。
- 与网页版关系：
  - 桌面端是中台的“正式客户端”，网页版保留为轻量入口。
  - 桌面端界面使用同一套 API，不直接读数据库。

### 3.4 SaaS 服务端控制面

- API 网关：
  - 统一鉴权（Token / API Key）
  - 租户上下文注入
  - 限流、超时、重试、审计
  - IP 白名单（企业可配置）
- 租户隔离：
  - 所有业务表带 `tenant_id`
  - 所有查询按当前租户过滤
  - 管理员可跨租户，普通用户不可越权
- 控制面服务：
  - 用户/角色/权限
  - 许可证与套餐
  - 用量统计与账单
  - 审计日志、操作日志
  - 通知、公告、版本发布

### 3.5 业务引擎（复用现有系统）

- 销售 Agent：会话记忆、客户档案、知识库 RAG、话术、转人工。
- CRM：线索、客户、商机、订单、回款、交付。
- 数据智能：经营总览、漏斗、ROI、趋势、团队、自定义报表。
- 销售流程：工作流、预测、派单、执行记录。
- 自动跟进：跟进计划、到期派发。
- 渠道：企业微信自建应用、微信客服、公众号、网页客服。
- 视觉执行器：服务端负责调度/去重/风控/审计，桌面端负责执行。

### 3.6 数据隔离

- 数据库层：统一 `tenant_id`，索引包含租户字段。
- Redis 层：Key 前缀 `tenant:{id}:`。
- 对象存储：目录按租户隔离，权限校验后才可访问。
- 文件导出：导出内容打水印/租户标识，审计可追溯。
- 数据删除：租户注销可一键清除/匿名化，满足合规。

### 3.7 安全合规

- 传输：全站 HTTPS，TLS 1.2+。
- 密钥：服务端只存密文；客户端用系统级加密存储；日志脱敏。
- 登录安全：失败锁定、验证码、异地登录提醒、会话可吊销。
- 操作审计：登录、配置、发送、导出、删除全部留痕。
- 内容安全：回复内容过敏感词/合规规则，转人工兜底。
- 风控：视觉执行器默认关闭，管理员授权；频控、熔断、人工接管。

### 3.8 商业化与计费

- 套餐：
  - 免费试用版：限账号数、限功能。
  - 专业版：官方渠道 + 基础自动化。
  - 企业版：多席位 + 视觉执行器 + 白标 + 专属部署。
- 计费维度：席位、租户数、API 用量、AI 调用量、存储量。
- 许可证：本地离线校验 + 服务端在线校验，防止破解。
- 增值：白标、私有化、SLA、专属客服。

---

## 4. 桌面端界面建议

```text
登录页：企业账号/API Key/邀请码
  ↓
工作台首页：今日经营、待跟进、告警、执行器状态
  ↓
左侧导航：
  经营总览 / 客户 / 线索 / 订单 / 数据智能 / 销售流程 / 自动跟进
  渠道接入（企微、微信客服、公众号）
  视觉执行器（开关 + 状态 + 审计）
  系统设置（成员、权限、IP 白名单、套餐）
```

关键交互：

- “视觉执行器”页面必须有开关、运行状态、今日发送、待执行任务、日志、暂停/恢复。
- 未登录/未授权时开关不可用，并提示原因。
- 关闭应用时执行器同步停止，避免后台残留。

---

## 5. 数据流示例

### 5.1 登录

```text
客户打开 .exe → 输入账号密码
  → 服务端校验 → 返回 access_token + 租户信息
  → 桌面端保存加密 Token → 进入本租户中台
```

### 5.2 查看数据

```text
桌面端请求 /api/v1/customers
  → 网关校验 Token → 注入 tenant_id
  → 业务层只查该租户数据 → 返回
```

### 5.3 启用视觉执行器

```text
管理员打开开关
  → 服务端记录授权状态
  → 桌面端启动本地执行器
  → 客户消息 → 截屏识别 → 中台生成回复 → 模拟发送 → 审计
  → 关闭开关/退出登录 → 执行器停止
```

---

## 6. 现有 vs 待建

| 模块 | 现状 | 商业级要求 |
|---|---|---|
| 业务引擎 | 已有 | 保留并收敛到统一 API |
| 多租户字段 | 已有 tenant_id | 补全所有表的隔离校验与测试 |
| 账号/登录 | 已有基础 | 增加注册、租户开通、RBAC、Token 刷新 |
| API 网关 | 已有 API Key 鉴权 | 增加 Token、限流、IP 白名单、审计中心 |
| 桌面客户端 | 框架已建（pywebview） | 商用版建议 Electron/Tauri + 自动更新 |
| 视觉执行器 | 已可跑通 | 纳入桌面开关、状态面板、审计联动 |
| 官网/下载/激活 | 无 | 需新建 |
| 许可证/计费 | 无 | 需新建 |
| 数据隔离测试 | 部分 | 需全量覆盖 |
| 自动更新 | 无 | 需新建 |

---

## 7. 里程碑

### P0（1-2 个月）：可售卖的 MVP

1. 官网 + 下载页 + 账号注册/登录/租户开通
2. 桌面客户端正式壳（登录 → 本租户中台 → 视觉执行器开关）
3. 全链路 `tenant_id` 隔离校验
4. 视觉执行器纳入开关、状态、审计
5. 许可证试用版 + 专业版

### P1（2-4 个月）：商业闭环

1. 自动更新 + 版本回滚
2. RBAC、IP 白名单、操作审计
3. 用量统计与计费
4. 内容安全/合规、数据导出/删除
5. 白标与品牌定制

### P2（4 个月+）：规模化

1. Electron/Tauri 正式客户端
2. 私有化部署版
3. SLA、灾备、多活
4. 生态 API / 开放平台

---

## 8. 风险与边界

- 视觉自动化存在风控/封号风险，必须在产品中明确授权、熔断、审计。
- 多租户隔离是商业底线，任何数据越权都是 P0 事故，需做专项测试。
- 许可证/计费必须服务端权威校验，客户端只做展示。
- 桌面端自动更新要支持断点、签名、回滚，避免版本事故。
